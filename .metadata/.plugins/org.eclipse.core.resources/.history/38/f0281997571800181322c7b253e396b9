package daa38.Statistics.Auxiliary;

public class SyncMemoryConsumptionTracker {
	private long mBytesConsumed;
	private boolean mTracking;
	
	public SyncMemoryConsumptionTracker()
	{
		mBytesConsumed = 0;
		mTracking = true;
	}
	
	public synchronized addBytes(long pBytes)
	{
		System.out.println("Add bytes called with "+pBytes);
		mBytesConsumed += pBytes;
	}
	
	public synchronized endTracking()
	{
		System.out.println("End tracking called "+pBytes);
		mBytesConsumed += ( Runtime.getRuntime().totalMemory() - Runtime.getRuntime().freeMemory() );
	}
	
	
}
